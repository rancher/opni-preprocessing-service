sonobuoy-config:
  driver: Job
  plugin-name: opni-sonobuoy
  result-format: junit
spec:
  image: 
  imagePullPolicy: Always
  name: plugin
  resources: { }
  volumeMounts:
    - mountPath: /tmp/results
      name: results